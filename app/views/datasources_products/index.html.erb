<h1>Listing datasources_products</h1>

<table>
  <tr>
    <th>Datasource</th>
    <th>Product</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @datasources_products.each do |datasources_product| %>
  <tr>
    <td><%= datasources_product.datasource.name %></td>
    <td><%= datasources_product.product.name  %></td>
    <td><%= link_to 'Show', datasources_product %></td>
    <td><%= link_to 'Edit', edit_datasources_product_path(datasources_product) %></td>
    <td><%= link_to 'Destroy', datasources_product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Datasources product', new_datasources_product_path %>
